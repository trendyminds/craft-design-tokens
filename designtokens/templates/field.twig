{% import "_includes/forms" as forms %}

{{ forms.hidden({
	value: field['config'],
	name: name ~ "[config]",
}) }}

{{ forms.selectField({
	id: id ~ namespacedId,
	name: "#{ name }[key]",
	options: options,
	value: value,
	first: true,
}) }}
